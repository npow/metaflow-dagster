name: UX Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * *"  # nightly — catches upstream metaflow regressions
  workflow_dispatch:

jobs:
  ux-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      METAFLOW_DEFAULT_METADATA: local
      METAFLOW_DEFAULT_DATASTORE: local
      METAFLOW_DEFAULT_ENVIRONMENT: local

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Pin Metaflow local datastore root
        run: echo "METAFLOW_DATASTORE_SYSROOT_LOCAL=$HOME" >> "$GITHUB_ENV"

      # Dagster runs jobs in-process via execute_in_process() — no external
      # scheduler service is required.

      - uses: Netflix/metaflow/.github/actions/run-ux-tests@npow/corral-devstack
        with:
          metaflow-ref: npow/corral-devstack
          extra-install: ".[test]"
          codecov-token: ${{ secrets.CODECOV_TOKEN }}
          ux-test-config: |
            backends:
              - name: dagster
                scheduler_type: dagster
                cluster: null
                decospec: null
                enabled: true
